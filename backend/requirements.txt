# Pinned so the API loads a pickle written by the same scikit-learn that trained
# it. A version mismatch here is the classic way a working notebook produces a
# backend that cannot load its own model.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
joblib==1.6.0
python-dotenv==1.2.3
pytest==9.1.1
httpx==0.28.1
